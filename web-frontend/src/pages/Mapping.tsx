import React from 'react';
import ConversationLayout from '../components/ConversationLayout';

const Mapping: React.FC = () => {
  return (
    <div className="mapping-page">
      <ConversationLayout conversationType="mapping" />
    </div>
  );
};

export default Mapping;
